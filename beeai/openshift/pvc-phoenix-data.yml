apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phoenix-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: crc-csi-hostpath-provisioner
  volumeMode: Filesystem
